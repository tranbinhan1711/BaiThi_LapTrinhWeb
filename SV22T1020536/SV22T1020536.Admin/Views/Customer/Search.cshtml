@model SV22T1020536.Models.Common.PagedResult<SV22T1020536.Models.Partner.Customer>
@{
    Layout = null;
}

<div class="mb-2 text-muted">
    Danh sÃ¡ch nÃ y cÃ³ <strong>@Model.RowCount</strong> khÃ¡ch hÃ ng. Trang <strong>@Model.Page/@Model.PageCount</strong>
</div>

<div class="card">
    <div class="card-body p-1">
        <table class="table table-bordered table-hover mb-0">
            <thead class="table-secondary">
                <tr>
                    <th>TÃªn khÃ¡ch hÃ ng</th>
                    <th>TÃªn giao dá»‹ch</th>
                    <th>Äiá»‡n thoáº¡i</th>
                    <th>Email</th>
                    <th>Äá»‹a chá»‰</th>
                    <th>Tá»‰nh / ThÃ nh</th>
                    <th class="text-center" style="width:120px;">Tráº¡ng thÃ¡i</th>
                    <th class="text-center" style="width:100px;">Thao tÃ¡c</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.DataItems)
                {
                    <tr>
                        <td>@item.CustomerName</td>
                        <td>@item.ContactName</td>
                        <td>@item.Phone</td>
                        <td>@item.Email</td>
                        <td>@item.Address</td>
                        <td>@item.Province</td>
                        <td class="text-center">
                            @if (item.IsLocked == true)
                            {
                                <span class="badge bg-danger">KhÃ³a</span>
                            }
                            else
                            {
                                <span class="badge bg-success">Hoáº¡t Ä‘á»™ng</span>
                            }
                        </td>
                        <td class="text-center text-nowrap">
                            <a href="~/Customer/Edit/@item.CustomerID" class="btn btn-sm btn-primary">
                                <i class="bi bi-pencil"></i>
                            </a>
                            <a href="~/Customer/Delete/@item.CustomerID" class="btn btn-sm btn-danger">
                                <i class="bi bi-trash"></i>
                            </a>
                            <a href="~/Customer/ChangePassword/@item.CustomerID" class="btn btn-sm btn-warning ms-2">
                                <i class="bi bi-key"></i>
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <div class="card-footer">
        @if (Model.PageCount > 1)
        {
            <ul class="pagination justify-content-center">
                @foreach (var p in Model.GetDisplayPages())
                {
                    if (p.Page == 0)
                    {
                        <li class="page-item disabled"><span class="page-link">...</span></li>
                    }
                    else if (p.IsCurrent)
                    {
                        <li class="page-item active"><span class="page-link">@p.Page</span></li>
                    }
                    else
                    {
                        <li class="page-item">
                            <a class="page-link" href="javascript:;" onclick="doSearch(@p.Page)">@p.Page</a>
                        </li>
                    }
                }
            </ul>
        }
    </div>
</div>
